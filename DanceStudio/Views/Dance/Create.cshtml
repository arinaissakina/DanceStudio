@model DanceStudio.Models.Dance

@{
    ViewData["Title"] = "Create";
}

<br/>
<p class="title is-1 is-spaced">Create</p>
<p class="subtitle is-3">Dance</p>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name" class="control-label"></label>
                <div class="field">
                    <div class="control">
                        <input asp-for="Name" class="input is-primary" type="text" placeholder="Primary input">
                    </div>
                </div>
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="button is-primary is-rounded" style="float: right"/>
            </div>
        </form>
    </div>
    <br/><br/><br/>
</div>
    <a asp-action="Index" class="button is-rounded">Back to List</a>
<div><br/>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
